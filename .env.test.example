# Loaded by tests/__init__.py with override=True, so this beats .env and
# pytest-env's fallback. Only DATABASE_URL belongs here; API_TOKEN comes from .env.
DATABASE_URL=postgresql+psycopg://budget:POSTGRES_PASSWORD@localhost:5432/budget_test
